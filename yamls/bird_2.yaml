# NOTE: For bird 2, syllables 'g' and 'a' were originally segmented as a single
# chunk 'a'. In the CSV, 'a' has been expanded to 'ga' in the song labels, but
# the onsets/offsets still reflect the original single-chunk segmentation
# (i.e., there is one onset/offset pair for the 'ga' chunk, not separate ones).

data_csv: "data/bird_2_df.csv"

transition_dir: "output/Transition matrices"
adjacent_dir: "output/Adjacent Correlations"
adjacent_dir_synth: "output/Synthetic Adjacent Correlations"
next_dir: "output/Next Correlations"
next_dir_synth: "output/Synthetic Next Correlations"
elasticity_path: "output/Elasticity"
emd_upcoming_path: "output/EMDs between repeat distributions/upcoming"
emd_preceding_path: "output/EMDs between repeat distributions/preceding"
repeat_distr_path: "output/Repeat distributions"
position_csvs_path: "output/Positions csvs"
time_csvs_path: "output/Time of day csvs"
correlations_by_distance_dir: "output/Correlations by distance"
heatmap_save_path: "figures/Figure 3"
adjacent_corr_combined_path: "output/Adjacent Correlations/Adjacent_Correlations_correction_bh.csv"
next_corr_combined_path: "output/Next Correlations/Next_Correlations_correction_bh.csv"
